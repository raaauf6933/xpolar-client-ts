import * as React from 'react';
import Button from '../Button';

const Filter: React.FC = () => {
  return (
    <>
      <div>
        {' '}
        <Button color="default">Filters</Button>
      </div>
    </>
  );
};

export default Filter;
